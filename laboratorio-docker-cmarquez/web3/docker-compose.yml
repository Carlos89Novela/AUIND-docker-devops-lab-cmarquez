services:
  web3:
    image: nginx:latest
    container_name: web3

    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro

    networks:
      - proxy

networks:
  proxy:
    external: true
