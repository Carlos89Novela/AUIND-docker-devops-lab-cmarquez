services:
  web1:
    image: nginx:latest
    container_name: web1

    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro

    networks:
      - proxy

networks:
  proxy:
    external: true